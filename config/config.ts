// import { defineConfig } from "umi";
// import proxy from './proxy';

// If you don't use defineConfig, you can get these configurations elsewhere
export default {
  plugins: [
  ],
  fastRefresh : true ,
  model : { } , 
  antd : { } , 
  request : { } , 
  initialState : { } ,
  // proxy,
  // dva: {},
  title: 'Dispenary',
  favicons: [
    '/favicon.svg'
  ]
};
